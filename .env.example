# Required in production — the server proxies one Claude API call per photo.
# Never expose this key to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional
PORT=3000

# Set to 1 to run without an API key (returns a canned analysis) for UI development.
MOCK_ANALYZE=0
